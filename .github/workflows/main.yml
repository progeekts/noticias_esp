name: Enviar Noticias Diarias
on:
  schedule:
    # Se ejecutará a las 09:00 y a las 20:00 UTC
    - cron: '0 9,20 * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout del código
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          # Eliminamos el caché temporalmente para asegurar una instalación limpia
          
      - name: Instalar dependencias
        run: |
          python -m pip install --upgrade pip
          pip install requests feedparser

      - name: Ejecutar script
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        # Aquí nos aseguramos de llamar al python del entorno correcto
        run: python noticias.py
